<% provide(:title, t("all-users")) %>
<h1><%= t"all-users" %></h1>

<%= paginate @users %>

<ul class="users">
  <% @users.each do |user| %>
    <%= render user %>
  <% end %>
</ul>

<%= paginate @users %>
